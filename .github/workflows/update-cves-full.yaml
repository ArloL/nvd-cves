name: Update CVEs (full)

on:
  workflow_dispatch:
  schedule:
  - cron: "34 4 * * *"
permissions: {}
concurrency: update-cves

jobs:
  update-cves:
    name: Update CVEs (full)
    permissions:
      contents: write
    uses: ./.github/workflows/cve-import.yaml
    secrets:
      NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
    with:
      full_import: true
